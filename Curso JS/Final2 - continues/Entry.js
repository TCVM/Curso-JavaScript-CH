class entrys{
    constructor(title, core, fecha, number,img){
       this.title = title; 
        this.core = core;
        this.fecha = fecha;
        this.number = number;
        this.img = img;
        
        //this.eliminar=function(){ aarray.splice(aarray.indexOf(this.number), 1); }  //otro intento de arreglar lo de la funcion eliminar.
    }
    //vi a oscar haciendo esto de separar  el constructor persona en un diferente .js y me parecio piola.
}